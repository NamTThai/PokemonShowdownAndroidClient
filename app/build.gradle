apply plugin: 'com.android.application'

android {
    compileSdkVersion 20
    buildToolsVersion '20.0.0'
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 20
        versionCode 1
        versionName '1.0.1'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    productFlavors {
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:support-v4:21.0.3'
    compile 'org.java-websocket:Java-WebSocket:1.3.0'
    compile files('libs/android-integration-3.1.0.jar')
    compile files('libs/PayPalAndroidSDK-2.9.0.jar')
}

task appStart(type: Exec, dependsOn: 'installDebug') {
    // linux
    def adb = android.plugin.extension.getAdbExe().toString()
    commandLine adb, 'shell', 'am', 'start', '-n', 'com.pokemonshowdown.app/.BattleFieldActivity'
}
